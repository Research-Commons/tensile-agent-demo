> ⚠️ **DEPRECATED (2026-09-23)** — this repository is no longer maintained. Example only; superseded by [`examples/11-hosted-agent/`](https://github.com/Research-Commons/tensile-sandbox-backend/tree/main/examples/11-hosted-agent) in tensile-sandbox-backend (same `agent.py` + `tensile.yaml`, with every manifest field documented). Copy that folder into a fresh repo when a public deploy target is needed. See `niko-monorepo/docs/20-repo-homes-tensile-view-and-personas.md` for the repo map.

# tensile-agent-demo

Minimal public demo agent for Tensile's hosted-agents surface: a `tensile.yaml`
with a one-line entrypoint that echoes its input as JSON. Used for platform
end-to-end verification.
